{
  "name": "docs",
  "$schema": "../node_modules/nx/schemas/project-schema.json",
  "projectType": "application",
  "root": "docs",
  "sourceRoot": "docs/src",
  "tags": ["type:docs"],
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "outputs": ["{workspaceRoot}/dist/docs"],
      "options": {
        "command": "astro build",
        "cwd": "docs"
      }
    },
    "serve": {
      "executor": "nx:run-commands",
      "continuous": true,
      "options": {
        "command": "astro dev --port 4300",
        "cwd": "docs"
      }
    },
    "preview": {
      "executor": "nx:run-commands",
      "continuous": true,
      "options": {
        "command": "astro preview --port 4300",
        "cwd": "docs"
      }
    }
  }
}
