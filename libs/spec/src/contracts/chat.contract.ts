import type { ComponentContract } from './types';

export const contract = {
  component: 'AtlChat',
  figmaNodeId: '507:2953',
  codeOnly: [
    {
      name: 'status',
      reason: "Master's own description: \"(code-only: status: idle | streaming | error, message handling)\".",
    },
  ],
} satisfies ComponentContract;
