import type { ComponentContract } from './types';

export const contract = {
  component: 'AtlTable',
  figmaNodeId: '421:1183',
  figmaOnly: [
    {
      name: 'error',
      reason:
        'UNEXPLAINED — surfaced by the S2 contract pass 2026-09-10; decide in tasks/todo.md',
    },
  ],
} satisfies ComponentContract;
