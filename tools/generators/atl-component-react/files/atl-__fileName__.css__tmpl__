/* ── <%= className %> ──────────────────────────────────────────────────── */

.atl-<%= fileName %> {
  display: block;
  font-family: var(--ui-font-family);
}

/* Variants */
.atl-<%= fileName %>.variant-default {
  /* default styles */
}
