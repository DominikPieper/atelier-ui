{
  "$schema": "http://json-schema.org/schema",
  "cli": "nx",
  "id": "atl-component-react",
  "title": "Atl React Component Generator",
  "description": "Scaffold a new LLM-optimized React component following the library conventions.",
  "type": "object",
  "properties": {
    "name": {
      "type": "string",
      "description": "Component name in kebab-case (e.g. 'tooltip', 'date-picker'). The 'atl-' prefix is added automatically.",
      "$default": { "$source": "argv", "index": 0 }
    },
    "directory": {
      "type": "string",
      "description": "Target directory relative to libs/react/src/lib/. Defaults to the component name."
    }
  },
  "required": ["name"]
}
