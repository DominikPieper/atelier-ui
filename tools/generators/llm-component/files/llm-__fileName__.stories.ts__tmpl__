import type { Meta, StoryObj } from '@storybook/angular';
import { argsToTemplate } from '@storybook/angular';
import { <%= className %> } from './llm-<%= fileName %>';

const meta: Meta<<%= className %>> = {
  title: 'Components/<%= className %>',
  component: <%= className %>,
  tags: ['autodocs'],
  render: (args) => ({
    props: args,
    template: `<<%= selector %> ${argsToTemplate(args)}>Label</<%= selector %>>`,
  }),
  argTypes: {
    variant: {
      control: 'select',
      options: ['default'],
    },
  },
  args: {
    variant: 'default',
  },
};

export default meta;
type Story = StoryObj<<%= className %>>;

export const Default: Story = {};

export const Playground: Story = {
  render: (args) => ({
    props: args,
    template: `<<%= selector %> ${argsToTemplate(args)}>Playground</<%= selector %>>`,
  }),
};
